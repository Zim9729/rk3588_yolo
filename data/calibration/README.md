# Calibration Data

For quantized RKNN conversion, place representative calibration images in this directory and create `dataset.txt` with one image path per line.

Example:

```text
data/calibration/image001.jpg
data/calibration/image002.jpg
```

Use images that match the target deployment scene as closely as possible.
